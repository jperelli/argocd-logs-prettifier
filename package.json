{
  "name": "argocd-jsonl-log-viewer",
  "version": "1.0.0",
  "description": "Chrome extension that renders JSON / logfmt log lines in the Argo CD pod Logs tab as structured, filterable rows.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node test/parser.test.js",
    "icons": "scripts/icons.sh",
    "package": "scripts/package.sh"
  }
}
